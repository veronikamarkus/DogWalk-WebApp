@model App.Domain.DogInWalk

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DogInWalk</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WalkId" class="control-label"></label>
                <select asp-for="WalkId" class="form-control" asp-items="ViewBag.WalkId"></select>
                <span asp-validation-for="WalkId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DogId" class="control-label"></label>
                <select asp-for="DogId" class="form-control" asp-items="ViewBag.DogId"></select>
                <span asp-validation-for="DogId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
