@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<table class="table table-striped">
@foreach (var claim in User.Claims)
{
    <tr>
        <td>Type: @claim.Type</td>
        <td>Value: @claim.Value</td>
    </tr>
}
</table>